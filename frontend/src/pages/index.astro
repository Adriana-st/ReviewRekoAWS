---
import Layout from '../layouts/Layout.astro';

---
<Layout>

  <div class="min-h-screen bg-slate-50">
    <div class="max-w-2xl mx-auto px-4 py-10">

      <!-- ── Header ─────────────────────────────────────────── -->
      <div class="mb-8">
        <h1 class="text-2xl font-semibold tracking-tight text-slate-900">ReviewReko</h1>
        <p class="mt-1 text-sm text-slate-400">Smart review moderation · AWS Rekognition</p>
      </div>

      <!-- ── Nav ───────────────────────────────────────────── -->
      <nav class="flex border-b border-slate-200 mb-8">
        <a id="nav-submit"  class="nav-tab">Submit Review</a>
        <a id="nav-gallery" class="nav-tab">Browse Reviews</a>
        <a id="nav-admin"   class="nav-tab">Admin</a>
      </nav>

      <!-- ══════════════ SUBMIT PAGE ══════════════ -->
      <div id="page-submit" class="page active space-y-4">

        <h2 class="text-base font-semibold text-slate-700">Submit a Product Review</h2>

        <!-- Your Details -->
        <div class="card">
          <p class="section-label">Your Details</p>
          <div>
            <label class="field-label">Your name</label>
            <input type="text" id="customerName" placeholder="e.g. Emma Byrne" class="field-input" />
          </div>
        </div>

        <!-- Product -->
        <div class="card">
          <p class="section-label">Product</p>
          <div>
            <label class="field-label">Product name</label>
            <input type="text" id="productName" placeholder="e.g. Nike Air Max" class="field-input" />
          </div>
          <div>
            <label class="field-label">Category</label>
            <select id="productCategory" class="field-input">
              <option value="">-- Select category --</option>
              <option value="footwear">Footwear</option>
              <option value="electronics">Electronics</option>
              <option value="clothing">Clothing</option>
              <option value="food">Food &amp; Drink</option>
              <option value="sports">Sports</option>
              <option value="homeware">Homeware</option>
              <option value="other">Other</option>
            </select>
          </div>
        </div>

        <!-- Review -->
        <div class="card">
          <p class="section-label">Your Review</p>
          <div>
            <label class="field-label">Star rating</label>
            <select id="starRating" class="field-input">
              <option value="5">★★★★★ (5)</option>
              <option value="4">★★★★☆ (4)</option>
              <option value="3">★★★☆☆ (3)</option>
              <option value="2">★★☆☆☆ (2)</option>
              <option value="1">★☆☆☆☆ (1)</option>
            </select>
          </div>
          <div>
            <label class="field-label">Review text</label>
            <textarea
              id="reviewText"
              rows="4"
              placeholder="Write your review here..."
              class="field-input resize-none"
            ></textarea>
          </div>
        </div>

        <!-- Image Upload -->
        <div class="card">
          <p class="section-label">Product Image</p>
          <label class="field-label">Upload a photo of the product</label>
          <input
            type="file"
            id="imageFile"
            accept="image/*"
            class="w-full text-sm text-slate-500
                   file:mr-4 file:rounded-lg file:border-0
                   file:bg-indigo-50 file:px-4 file:py-2
                   file:text-sm file:font-medium file:text-indigo-700
                   hover:file:bg-indigo-100 transition"
          />
          <img
            id="preview-img"
            alt="Preview"
            class="hidden mt-3 w-40 rounded-xl border border-slate-100 object-cover shadow-sm"
          />
        </div>

        <button id="btn-submit-review" class="btn-primary w-full">
          Submit Review
        </button>

        <p id="status-msg" class="min-h-5 text-center text-sm text-slate-500"></p>
      </div>

      <!-- ══════════════ GALLERY PAGE ══════════════ -->
      <div id="page-gallery" class="page space-y-5">

        <div class="flex items-center justify-between">
          <h2 class="text-base font-semibold text-slate-700">Browse Approved Reviews</h2>
          <button id="btn-load-gallery" class="btn-primary">Load Reviews</button>
        </div>

        <div class="flex items-center gap-3">
          <label class="text-sm font-medium text-slate-600">Filter by category</label>
          <select id="filter-category" class="field-input w-auto">
            <option value="">All categories</option>
            <option value="footwear">Footwear</option>
            <option value="electronics">Electronics</option>
            <option value="clothing">Clothing</option>
            <option value="food">Food &amp; Drink</option>
            <option value="sports">Sports</option>
            <option value="homeware">Homeware</option>
            <option value="other">Other</option>
          </select>
        </div>

        <div id="gallery-results" class="space-y-4">
          <p class="text-sm text-slate-400">Click a category or load all reviews.</p>
        </div>
      </div>

      <!-- ══════════════ ADMIN PAGE ══════════════ -->
      <!--
      <div id="page-admin" class="page space-y-4">
        <h2 class="text-base font-semibold text-slate-700">Admin — All Submissions</h2>
        <p class="text-xs text-slate-400">Shows all reviews including rejected ones.</p>
        <button id="btn-load-admin" class="btn-primary">Load All Records</button>
        <div id="admin-results" class="space-y-3"></div>
      </div>
      -->

    </div>
  </div>


<style>
  /* ── Page visibility — toggled by JS ── */
  .page         { display: none; }
  .page.active  { display: block; }

  /* ── Nav tabs ── */
  .nav-tab {
    @apply cursor-pointer border-b-2 border-transparent px-5 py-2.5
           text-sm font-medium text-slate-400 transition-colors
           hover:text-slate-700;
  }
  .nav-tab.active {
    @apply border-indigo-500 text-indigo-600;
  }

  /* ── Reusable card ── */
  .card {
    @apply space-y-4 rounded-2xl border border-slate-100 bg-white p-6 shadow-sm;
  }

  /* ── Section label inside a card ── */
  .section-label {
    @apply text-xs font-semibold uppercase tracking-widest text-slate-400;
  }

  /* ── Form label ── */
  .field-label {
    @apply mb-1 block text-sm font-medium text-slate-700;
  }

  /* ── All text inputs, selects, textareas ── */
  .field-input {
    @apply w-full rounded-lg border border-slate-200 bg-white px-3 py-2
           text-sm text-slate-900 placeholder-slate-400 transition
           focus:border-transparent focus:outline-none focus:ring-2 focus:ring-indigo-500;
  }

  /* ── Primary button ── */
  .btn-primary {
    @apply rounded-xl bg-indigo-600 px-5 py-2.5 text-sm font-semibold
           text-white shadow-sm transition-colors hover:bg-indigo-700;
  }
</style>



</Layout>